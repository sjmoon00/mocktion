# AI 협업 로그 (AI Collaboration Log)

> 이 프로젝트의 목표 중 하나는 "무엇을 만들었는가"뿐 아니라 "Claude Code와 어떻게 협업해서 만들었는가"를 그 자체로 기록하는 것이다.
> [DECISIONS.md](DECISIONS.md), [TRADEOFFS.md](TRADEOFFS.md)가 "결과에 대한 근거"라면, 이 문서는 **시간순 과정** — 무엇을 물었고, AI가 무엇을 하고 무엇을 놓쳤고, 사람이 어디서 판단을 개입시켰는지 — 를 담는다.
> 세션(대화 단위)마다 항목을 추가한다. 완료된 항목을 지우거나 다시 쓰지 않는다 — 기록이므로 누적한다.

---

## 세션 1 — 2026-07-09: Phase 0 계획 확정 및 실 DB 검증

**시작 상태**: `notion-mockserver-spec.md`(기획서)와 `IMPLEMENTATION_PLAN.md`(Claude Code가 기획서를 검토해 만든 세부 구현 계획)만 존재. 코드 없음.

**진행 과정**:

1. "Phase 0 구현 계획을 먼저 확정해봐"라는 요청에, AI가 계획서 체크리스트 항목을 실행 가능한 수준까지 구체화 — 모듈 시스템(CJS vs ESM), tsconfig 세부값, package.json 필드 등. 이 과정에서 AI가 `npm view @notionhq/client`로 패키지 메타데이터를 직접 조회해 "계획서에 NodeNext라고만 적혀 있던 것"을 "CJS 기반 NodeNext"로 구체적인 결정으로 확정함 ([D-005](DECISIONS.md#d-005-commonjs--moduleresolution-nodenext-esm-미채택)).
2. Phase 0의 나머지 항목(실 DB 연결 검증)은 AI가 임의로 진행할 수 없는 지점이었다 — 실제 `NOTION_TOKEN`과 대상 Notion DB URL이 필요했다. AI가 이 지점에서 멈추고 사용자에게 준비 방법을 질의함 (AskUserQuestion). **사람이 개입한 지점**: 토큰을 대화창에 직접 붙여넣지 않고 `.env` 파일에 직접 기록한 뒤 알려주는 방식을 선택 — 민감정보를 대화 로그에 남기지 않기 위한 사용자의 판단.
3. 사용자가 `.env` 준비를 알리자, AI가 임시 탐색 스크립트(`_phase0-explore.ts`, 이후 삭제)로 실제 Notion DB(대회/팀 관리 서비스, 페이지 120개)에 연결해:
   - `databases.retrieve` → `data_sources` 배열 확인
   - 전체 페이지 페이지네이션 순회 (100개 제한 실제로 발동 — 120개 중 20개는 페이지네이션 없이는 누락됐을 것)
   - 스펙 문서가 이미 검증했다고 언급한 실제 4개 예시 페이지(GET 제출물 상세조회 / POST 팀원 등록 / PUT 정렬 저장 / DELETE 카테고리 삭제)를 정확히 찾아내 fixture로 저장
4. **AI가 실측으로 잡아낸 것 — 스펙 문서의 가정과 실제 데이터가 다른 3가지**:
   - `HTTP Method`/`응답코드`가 스펙 가정(Select)과 달리 실제로는 `multi_select`
   - `URI`가 스펙 가정(Title)과 달리 `rich_text`이고, 진짜 Title 프로퍼티는 `기능명`(한글 설명)
   - "예외 상황" 텍스트가 스펙 가정(`paragraph`)과 달리 실제로는 `bulleted_list_item` 블록
   
   이 세 가지는 스펙 문서를 아무리 정독해도 코드로 옮기기 전엔 드러나지 않는 것들이었다. Phase 1(파싱 모듈 구현)에 들어가기 전에 Phase 0에서 실 DB로 먼저 검증하는 단계를 계획에 넣어둔 것 자체가, 발견을 앞당긴 핵심 지점이다.
5. 발견 직후 AI가 `IMPLEMENTATION_PLAN.md`의 Phase 1 체크리스트 문구 자체를 실제 스키마 기준으로 다시 썼다 (예: "Method(select) 추출" → "Method는 multi_select 배열의 첫 값 사용, 0개면 스킵+경고, 2개 이상이면 경고 후 첫 값"). 발견을 별도 메모로만 남기지 않고 실행 체크리스트에 직접 반영해, 나중에 계획과 실제 구현이 따로 노는 것을 방지.

**포트폴리오 관점에서의 핵심 포인트**:
- AI에게 "스펙대로 구현해"라고 맡기지 않고, "실 데이터로 가정을 먼저 검증하는 단계"를 Phase 0으로 설계에 넣은 것 — 이건 AI가 아니라 사람의 판단이었다.
- AI가 실제로 스키마 불일치 3건을 찾아냈지만, 그걸 그대로 밀어붙이지 않고 계획 문서에 근거와 함께 기록하도록 지시한 것도 사람의 개입.
- 토큰 등 민감정보를 AI와의 대화창에 노출하지 않는 방식(.env 직접 작성)을 사용자가 스스로 선택.

---

## 세션 2 — 2026-07-09: Phase 1 파싱 모듈 구현

**시작 상태**: Phase 0 완료 — `notionClient.ts`, 실 DB 검증 결과, fixture 4개(`get-200-submission-detail`, `post-201-team-member`, `put-204-sort`, `delete-204-category`) 확보됨.

**진행 과정**:

1. "Phase 1 구현 시작해줘"라는 짧은 요청에, AI가 바로 코드를 쓰지 않고 `IMPLEMENTATION_PLAN.md`/`DECISIONS.md`/`notion-mockserver-spec.md`와 fixture 4개 전체를 먼저 읽어 계획의 세부 규칙(3단계 우선순위, 에러 테이블 파싱 규칙, `__children` 필드로 미리 펼쳐진 fixture 구조)을 파악한 뒤 설계에 들어감.
2. 구현 전 `@notionhq/client`(v5.23.0) 타입 선언을 직접 뒤져 계획서가 몰랐던 공식 헬퍼 3가지를 발견: `extractDatabaseId`(URL→ID 추출), `isFullPage`/`isFullBlock`(partial 응답 타입 좁히기), `collectPaginatedAPI`(페이지네이션 제네릭 헬퍼). 계획서의 "자체 정규식으로 32hex 추출" 항목을 SDK 헬퍼 재사용으로 대체함([D-009](DECISIONS.md#d-009-db-url--database_id-추출은-자체-정규식-대신-sdk의-extractdatabaseid-헬퍼를-래핑)).
3. `collectPaginatedAPI`를 실제로 적용했다가 `npm run build`(strict TS)에서 타입 에러 발견 — SDK 자체의 `start_cursor: string | null | undefined` 타입이 헬퍼의 제네릭 제약(`string | undefined`)과 불일치. 제네릭 타입 인자를 억지로 맞추는 대신 계획서 원안인 수동 `has_more` 루프로 되돌림([D-011](DECISIONS.md#d-011-notion-페이지네이션은-sdk의-collectpaginatedapi-대신-수동-has_morenext_cursor-루프)) — "타입 에러를 우회하기보다 더 단순한 대안으로 교체"한 판단.
4. `--status` 필터는 계획서의 "select/status 타입 감지 후 서버사이드 필터 분기" 대신, 전량 조회 후 클라이언트 사이드에서 값을 비교하는 방식으로 단순화([D-010](DECISIONS.md#d-010---status-필터는-서버사이드-selectstatus-분기-대신-전량-조회-후-클라이언트-사이드-필터링)) — Phase 0에서 이미 페이지 수(120개)와 상태 타입(`status` 고정)이 확인되어 서버사이드 분기의 이득이 크지 않다고 판단. **사람이 사후 검토해야 할 지점**: 이 단순화는 AI가 자체 판단(Auto Mode)으로 진행한 것이라, 사용자가 이 트레이드오프에 동의하는지 확인이 필요하다.
5. 6개 모듈(`richText`, `databaseFetcher`, `propertyExtractor`, `blockParser`, `responseJsonExtractor`, `errorCaseParser`) + 4개 테스트 파일을 구현. fixture의 `__children` 필드명을 그대로 내부 타입(`BlockWithChildren`)에 사용해, 실 데이터 구조와 파서 타입이 그대로 맞물리도록 설계.
6. `npm test`(18개 테스트, GET/POST/PUT/DELETE 4개 fixture 기반) + `npm run build`(strict TS) 모두 통과 확인.

**포트폴리오 관점에서의 핵심 포인트**:
- "계획서에 없던 공식 SDK 헬퍼를 찾아 재사용"과 "그 헬퍼가 실제로는 타입이 안 맞아 되돌림" 둘 다 같은 세션에서 일어남 — AI가 라이브러리를 맹신하지 않고 컴파일러 피드백으로 재검증한 사례.
- 계획서의 서버사이드 필터 분기 로직을 통째로 단순화한 결정(D-010)은 사람의 사전 승인 없이 Auto Mode 하에서 AI가 내린 판단이므로, 다음 세션에서 사용자 검토가 필요.

---

## 세션 3 — 2026-07-09/10: PR #1 코드 리뷰 대응

**시작 상태**: `Feature/Phase1-NotionParser` 브랜치, PR #1 오픈. `pr-review` 스킬로 Claude `/code-review high`(8개 앵글) + GitHub Copilot 인라인 코멘트를 종합한 `docs/reviews/2026-07-09-phase1-code-review.md`가 이미 작성되어 있는 상태에서 시작.

**진행 과정**:

1. 리뷰 문서를 읽고 AI가 8개 findings를 심각도 순으로 정리해 "6개 커밋 단위 + 문서화" 계획을 제시. 이 시점엔 리뷰 문서의 주장을 그대로 신뢰하고 커밋 그룹으로만 재배열했을 뿐, 실제 소스와 대조 검증은 하지 않은 상태였다.
2. **사람이 개입한 지점**: "리뷰들이 받아들일만한지 검토한거지?"라는 질문으로 AI가 검증을 생략했다는 것을 정확히 짚어냄. AI는 이를 인정하고, 8개 findings 전부를 현재 `src/notion/*.ts` 소스와 한 줄씩 대조하는 재검증을 수행 — 라인 번호·동작 설명이 실제 코드와 일치하는지, 리뷰가 스스로 하향 조정한 항목(#5)의 판단 근거(`richText.ts`의 null 가드, Notion 테이블 데이터 모델)가 타당한지 확인. 결과: 8건 모두 사실관계 일치, 오탐 없음.
3. Finding #3(errorCaseParser 비재귀 탐색)의 리뷰 자체의 Open Question — "예외 상황/테이블 중첩이 실 DB에 실제로 있는가" — 을 검증하기 위해 `_review-verify.ts`(Phase 0 방식과 동일하게 임시 스크립트, 확인 후 삭제)로 실 DB 120페이지 전체를 순회. 결과: 중첩 사례 0건. 재귀/비재귀 결과가 갈린 7개 페이지도 전부 응답코드 셀에 선행 정수가 없어 정상 skip된 것으로, 중첩과 무관함을 확인.
4. 이 실측 결과를 반영해 Finding #3의 우선순위를 Medium → 방어적 개선(사실상 Low)으로 재조정하고, 최종 7개 커밋 계획을 확정해 사용자 승인을 받음.
5. 승인된 계획대로 6개 코드 커밋(페이지네이션 방어 가드, JSON 실패 폴백, 예외 상황 재귀 탐색, 상태 필터링 분리, `__children` 캐스팅 제거, 표시명 헬퍼 통합) + 문서화 커밋을 CLAUDE.md의 "구현 계획과 커밋 단위" 규칙대로 각 단위 완료·빌드·테스트 통과 즉시 그 자리에서 커밋. 최종 51개 테스트, strict 빌드 통과.

**포트폴리오 관점에서의 핵심 포인트**:
- AI가 리뷰 결과를 검증 없이 그대로 실행 계획으로 옮긴 것을, 사람이 "정말 검토한 게 맞느냐"는 한 마디로 잡아낸 사례 — AI 출력물(코드 리뷰 문서)도 무비판적으로 신뢰하면 안 된다는 것을 보여준다.
- "코드가 비대칭적으로 짜여 있다"는 사실과 "실전에서 문제가 되는가"는 별개 질문이라는 점을 실 DB 재현으로 직접 확인한 사례(D-014) — 리뷰의 지적이 유효해도, 고칠지 말지는 실측 데이터로 판단해야 한다는 이 프로젝트의 반복되는 원칙(Phase 0의 스펙-실데이터 불일치 3건과 같은 맥락)이 코드 리뷰 대응에도 그대로 적용됨.

---

## 세션 4 — 2026-07-10: Phase 2 목서버 + CLI 통합 구현

**시작 상태**: PR #1 머지 완료, Phase 1(파싱 모듈 6개 + 테스트 51개) 완료. `src/mock/`, `pageParser.ts` 모두 미착수.

**진행 과정**:

1. "Phase 2 세부 구현 계획 작성해봐"라는 요청에 앞서, AI가 `docs/IMPLEMENTATION_PLAN.md`의 "프로젝트 구조" 절과 실제 `src/notion/` 파일 목록을 대조해 계획 상의 간극을 하나 발견함 — 구조 설명에는 `pageParser.ts`(페이지→`EndpointSpec` 오케스트레이터)가 명시돼 있는데, Phase 1 체크리스트에는 이 파일 작성이 아예 빠져 있었고 실제로 존재하지 않았다. Phase 2가 `index.ts`에서 파싱 결과를 조립하려면 이 오케스트레이터가 선행돼야 하므로, 계획에 0번 유닛으로 끼워 넣고 그 사실을 사용자에게 먼저 알림.
2. 4개 커밋 단위(`pageParser.ts` → `routeRegistry.ts` → `server.ts` → `index.ts` 통합) 계획을 제시하고 승인받은 뒤, CLAUDE.md 규칙대로 각 유닛을 완료·`tsc --noEmit` 통과 즉시 그 자리에서 커밋(4개 커밋).
3. `routeRegistry.ts` 구현 중 중복 엔드포인트 감지 경고 문구를 "나중에 등록된 것이 우선 적용됩니다"로 초안 작성했다가, Express가 동일 path에 동일 메서드로 두 번 등록되면 첫 번째 핸들러가 요청을 가로챈다는 사실을 반영해 "먼저 등록된 것이 우선 적용됩니다"로 스스로 정정 — 계획 문서에 없던 세부 동작이라 구현 중 확인 필요했던 지점.
4. 구현 완료 후 단위 테스트/빌드만으로 끝내지 않고, `.env`에 있는 실제 DB_URL로 목서버를 직접 기동해 E2E 검증(계획서의 확정된 스코프 — 목서버 계열은 fixture 테스트 대상이 아니라 수동/E2E 검증). 실 DB 120페이지 기준 119개 엔드포인트·365개 에러 케이스 등록 확인, `curl`로 GET 200 JSON·DELETE 204 무본문·CORS 헤더(`Access-Control-Allow-Origin: *`)·미등록 경로 404(등록 엔드포인트 119개 힌트 포함) 4가지를 모두 실측으로 통과시킴.
5. E2E 도중 흥미로운 확인: 계획서 시나리오였던 "본문 미문서화 POST → 빈 객체 `{}`" 케이스를 실제로 재현하려고 골랐던 `POST /teams`가, 알고 보니 실제로는 `{"teamId": 1}`이라는 JSON 예시를 페이지에 갖고 있어 그 값 그대로 반환됨 — 파서가 3순위(빈 객체) 폴백이 아니라 2순위(응답 JSON 그대로 사용)로 정확히 분기한 것. 3순위 폴백 자체는 이미 Phase 1 fixture 테스트(`post-201-team-member`)로 별도 검증돼 있어 문제는 아니었음.
6. `docs/IMPLEMENTATION_PLAN.md`의 Phase 2 체크박스와 E2E 검증 체크박스를 실제 확인 결과로 갱신(`pageParser.ts` 추가 항목 포함).

**포트폴리오 관점에서의 핵심 포인트**:
- 계획 문서 자체의 빈틈(`pageParser.ts` 누락)을 구현 착수 전 발견하고, 조용히 채워 넣지 않고 사용자에게 먼저 알린 뒤 계획에 반영한 사례.
- "테스트 통과 = 완료"로 끝내지 않고, 이 프로젝트가 확정한 스코프(목서버는 E2E 검증)를 실제로 실 DB에 대고 수행해, 문서상 시나리오와 실 데이터가 다르게 흘러가는 지점(POST /teams의 JSON 예시 존재)까지 확인한 것 — Phase 0/세션 3에서 반복된 "실측으로 확인한다" 원칙이 여기서도 그대로 적용됨.

---

## 세션 5 — 2026-07-10: PR #2(Phase 2) 코드 리뷰 검증 및 대응

**시작 상태**: PR #2(`Feature/Phase2-MockServer`) 오픈, `docs/reviews/2026-07-10-phase2-mockserver-cli-code-review.md`(Claude 8앵글 + Copilot 인라인 코멘트 종합, 10개 findings) 이미 작성됨. **직전 세션(세션 4 말미)에 사용자가 CLAUDE.md에 "리뷰 결과물도 실 소스와 대조 검증한다" 규칙을 직접 추가한 상태**라, 이번 세션이 그 규칙이 실제로 적용되는 첫 사례가 됨.

**진행 과정**:

1. "리뷰 문서를 보고 받아들일만 한지 먼저 검토해봐"라는 요청에, AI가 문서의 주장을 그대로 요약하지 않고 10개 finding 전부를 현재 소스(`routeRegistry.ts`, `server.ts`, `pageParser.ts`, `blockParser.ts`, `index.ts`)와 라인 단위로 대조. 재현 가능한 4건(#1 URI 변환 크래시, #3 listen 에러 미처리, #5 중복 감지 우회, #6 `--port` 늦은 실패)은 실제 Node/Express 스크립트로 직접 재현해 검증 — 리뷰 문서에 적힌 재현 결과(예: `registered /users/{user-id} without throwing` 문구)와 AI의 독립 재현 결과가 정확히 일치함을 확인.
2. 재현 과정에서 리뷰 문서의 자체 교정 내역(Finding #6 — Claude 초기 앵글들의 "조용한 포트 폴백"/"크래시" 과장 주장을 리뷰가 스스로 REFUTED로 정정한 부분)까지 별도로 재현해, 그 최종 결론(Low, 단순 UX 이슈)이 맞다는 것도 확인함 — 리뷰 문서 자체의 자기 수정 과정도 무비판적으로 받아들이지 않고 검증함.
3. 이미 확보해둔 세션 4의 실 DB E2E 로그(119개 엔드포인트 목록)를 재활용해, 리뷰의 Open Question(하이픈 등 `\w` 밖 문자를 쓰는 URI 파라미터가 실 DB에 실존하는가)에 추가 조회 없이 답변 — 실존하지 않음을 확인, Finding #1/#4/#5는 "실재하는 버그이지만 현재 실 데이터에서는 아직 발현되지 않은 잠재 결함"으로 결론.
4. 검증 결과 10개 finding 전부 라인/설명 정확, 재현 주장도 전부 사실로 확인 — 리뷰 문서를 신뢰할 만하다고 판단하고 사용자에게 보고.
5. "수정 계획 세워봐"라는 후속 요청에, finding 1개당 커밋 1개(세션 3의 전례)를 기계적으로 따르지 않고 **같은 짧은 함수(`createMockServer`의 for 루프) 안에 여러 finding이 몰려 있다는 점을 근거로 4개 커밋으로 재구성** — 특히 Finding #4(등록된 엔드포인트만 카운트)는 `createMockServer`의 반환 타입을 바꿔야 해서 `server.ts`를 함께 고치지 않으면 그 사이 지점에서 빌드가 깨진다는 것을 미리 지적하고 두 파일을 한 커밋으로 묶음.
6. Finding #10(switch문 스타일)은 리뷰 문서 스스로 "바꿀 실익 크지 않음"이라 적어둔 것을 그대로 존중해 스코프에서 제외 — CLAUDE.md의 "동작하는 코드 임의 리팩토링 금지" 원칙과 일치시킴.
7. 계획 승인 후 4개 커밋(URI 변환/등록 실패 방어/중복 감지, 등록된 스펙 추적/listen 에러, 페이지 파싱 후반부 에러 처리 통일, `--port` 조기 검증)을 각 유닛 완료·`tsc --noEmit`·`npm test`(51개) 통과 즉시 커밋. High severity였던 #3(listen 에러)과 #4(`--port` 조기 검증)는 커밋 직후 재현 스크립트로 수정이 실제로 동작하는지(포트 충돌 시 raw stacktrace 대신 일관된 에러 메시지, `--port abc` 시 Notion API 호출 전에 즉시 실패) 다시 한번 실측 확인.

**포트폴리오 관점에서의 핵심 포인트**:
- 세션 4에서 사람이 CLAUDE.md에 추가한 규칙("리뷰 결과물도 실 소스와 대조 검증")이, 바로 다음 세션에서 AI 자신의 행동을 실제로 바꾸는 것으로 이어진 사례 — 규칙이 문서에만 남지 않고 실제 워크플로우에 반영됨을 보여준다.
- "리뷰가 검증됐다고 적혀 있다"와 "실제로 검증됐다"는 다른 문제라는 걸 이번엔 직접 재현으로 확인했고, 그 결과 리뷰 문서가 실제로 신뢰할 만하다는 것도 확인함 — 세션 3(검증 없이 신뢰했다가 지적받음)과 세션 5(직접 검증해서 신뢰 가능함을 확인)를 대비하면, "검증한다"는 원칙이 매번 리뷰를 불신하라는 뜻이 아니라 매번 확인 절차를 거치라는 뜻이라는 걸 보여주는 사례.

---

## 세션 6 — 2026-07-10: Phase 3(마무리) 세부 계획 수립 및 구현

**시작 상태**: Phase 2 완료·병합됨(PR #2). `IMPLEMENTATION_PLAN.md`의 Phase 3 체크리스트는 계획서 초안 그대로("429 재시도", "에러 메시지", "빌드 확인", "README", "bin 필드(선택)") 다섯 줄뿐, 세부 계획 없음.

**진행 과정**:

1. "Phase 3 구현을 위한 세부계획 작성 시작해줘"라는 요청에, 계획서 문구를 그대로 체크리스트로 옮기지 않고 **실제 코드 상태부터 대조 확인**. 그 결과 계획서 작성 시점엔 몰랐던 두 가지를 발견함:
   - `node_modules/@notionhq/client`(v5.23.0)의 `Client.js` 소스를 직접 읽어, 429/529에 대한 `Retry-After` 헤더 존중 재시도가 이미 SDK에 내장되어 있음(기본 `maxRetries: 2`)을 확인. 계획서가 전제한 "재시도 로직을 새로 구현해야 한다"는 가정이 틀렸다는 것을 실제 라이브러리 코드로 검증.
   - `notionClient.ts`/`databaseFetcher.ts`를 다시 읽어, "잘못된 URL/멀티 데이터소스/토큰 누락" 에러 메시지 세 가지 모두 Phase 0~1에서 이미 구현되어 있었음을 확인(단, `resolveDataSourceId`는 유닛 테스트가 없었음).
   → 이 두 발견 덕분에 Phase 3의 실제 남은 작업량이 계획서가 암시한 것보다 훨씬 적다는 것을 구현 착수 전에 미리 확정할 수 있었음 — Phase 0에서 확립된 "스펙 문서 가정을 실측으로 검증한다" 습관이 이번엔 "계획서 자체의 가정"에 적용된 사례.
2. `package.json`의 `bin` 필드(계획서에 "(선택)"으로 명시됨)는 AI가 임의로 넣거나 빼지 않고, 먼저 그게 무엇을 하는지(npx/전역 설치 실행)와 지금 추가해도 실익이 없는 이유(npm publish 계획 없음)를 설명한 뒤 사용자에게 직접 결정하도록 질의(AskUserQuestion). **사람이 개입한 지점**: "스킵(권장)"을 선택 — AI의 권장을 그대로 받아들였지만, 결정 자체는 사람이 함. 이 결정은 `docs/TRADEOFFS.md` T-009로 기록.
3. 계획을 `docs/IMPLEMENTATION_PLAN.md`에 커밋 경계 단위(Unit 1~5)로 직접 반영한 뒤, 사용자가 "계획을 내가 확인 가능하게 전체를 보여줘"라고 요청 — AI가 요약하지 않고 문서에 실제로 반영된 원문을 그대로 인용해 보여줌(요약과 실제 반영 내용이 어긋나지 않게 하기 위함).
4. "패턴대로 브랜치 생성하고 구현 시작해"라는 요청에, AI가 임의로 브랜치 이름을 짓지 않고 `git log --all --graph`로 기존 병합 이력을 확인해 `Feature/Phase{N}-{모듈명}` 패턴(`Feature/Phase1-NotionParser`, `Feature/Phase2-MockServer`)을 먼저 확인한 뒤 `Feature/Phase3-Finalize`로 명명.
5. Unit 1(429 재시도 옵션)·Unit 2(`resolveDataSourceId` 테스트 4건 추가 + 실제 CLI로 토큰 누락/잘못된 URL 시나리오 육안 확인)·Unit 4(클린 빌드 + 컴파일된 `dist/index.js`를 실 DB로 재기동해 curl로 404/CORS/실응답 확인)·Unit 5(README 신규 작성)를 계획한 순서대로 각 단위 완료 즉시 커밋. 실 DB E2E는 108페이지 순차 파싱이라 시간이 걸려 백그라운드로 실행해두고 다른 단위(README 작성) 작업을 병행함.
6. Unit 4 실행 중 흥미로운 부수 발견: 실 DB가 팀이 계속 편집하는 라이브 문서라, 세션 4(하루 전)의 E2E 검증 당시 119개였던 등록 엔드포인트 수가 이번엔 107개로 달라져 있었음 — 코드 결함이 아니라 실제 데이터가 변경된 것임을 확인하고 `IMPLEMENTATION_PLAN.md`에 그 이유를 명시적으로 기록(과거 기록은 덮어쓰지 않고 새 결과에 이유를 덧붙임).

**포트폴리오 관점에서의 핵심 포인트**:
- "계획서에 적힌 대로 구현하면 된다"고 가정하지 않고, 착수 전에 실제 라이브러리 소스와 기존 코드를 먼저 대조해 계획서의 두 가지 전제가 이미 틀렸다는 것을 스스로 찾아낸 사례 — Phase 0의 "스펙 대 실데이터 검증" 습관이 "계획 대 실코드 검증"으로 확장 적용됨.
- 선택 사항(`bin` 필드)에 대해 AI가 장단점만 설명하고 결정은 사람에게 넘긴 것, 그리고 브랜치 명명을 추측하지 않고 실제 git 이력에서 패턴을 확인한 것 — 둘 다 "확실하지 않으면 실제 상태를 먼저 확인하고, 사람이 결정할 지점은 사람에게 맡긴다"는 동일한 원칙의 다른 적용.
- 실 DB 수치가 세션 간 달라진 것을 발견했을 때 이를 조용히 덮어쓰거나 무시하지 않고, 원인(라이브 문서의 자연스러운 변경)을 확인한 뒤 문서에 그대로 남긴 것 — "실측 검증"이 한 번으로 끝나는 절차가 아니라 매 세션 다시 확인해야 하는 것임을 보여주는 사례.
- 커밋 단위를 "finding 1개 = 커밋 1개"라는 기계적 규칙이 아니라, 실제 코드 의존관계(같은 함수, 반환 타입 변경으로 인한 빌드 순서)에 맞춰 재구성한 판단.

---

## 세션 7 — 2026-07-11: 실사용 피드백(파싱 경고 로그 페이지 식별 불가) 대응 및 프로세스 규칙 보강

**시작 상태**: Phase 3 완료·머지됨. 사용자가 Phase 3 이후 처음으로 실 DB(`--db` 실제 URL)를 직접 실행해보다가, 파싱 경고 로그(`⚠️ 응답코드에서 숫자를 찾을 수 없어...` 등)가 정확히 어느 Notion 페이지에서 발생했는지 알 수 없다는 실사용 문제를 발견하고 세션을 시작함.

**진행 과정**:

1. 사용자가 붙여넣은 실행 로그를 근거로, AI가 원인을 추적 — `pageParser.ts`가 페이지 단위로 순회하며 페이지 제목(`displayName`)과 URL(`page.url`)을 이미 갖고 있음에도, 실제로 경고를 출력하는 `responseJsonExtractor.ts`/`errorCaseParser.ts`/`propertyExtractor.ts`의 함수들에는 이 정보가 전달되지 않고 있음을 소스 대조로 확인. 해결 방향(제목+URL을 `pageLabel`로 결합해 옵션 파라미터로 하위 전달, 기존 테스트 하위호환 유지)을 채팅 텍스트로 제시하고 승인을 요청.
2. **사람이 개입한 지점**: 사용자가 이 제안 내용이 아니라 **제안 방식 자체**를 지적 — "CLAUDE.md를 보면 작업 과정을 먼저 정리하고 프로그레스 추적하면서 하는 과정이 선행되어야 하는데 왜 규칙을 무시하냐"는 질문. AI는 CLAUDE.md의 "구현 계획과 커밋 단위" 규칙이 계획 수립·승인·커밋 절차는 규정하면서도, 그 계획을 `docs/IMPLEMENTATION_PLAN.md`(실행의 단일 진실 공급원)와 TaskCreate 트래커에 실제로 반영해야 한다는 것까지는 명문화하지 않았다는 점을 인정. 채팅 텍스트로만 계획을 나열하고 넘어간 것이 규칙 위반이었음을 시인.
3. 지적을 받은 즉시 프로세스부터 재정비 — `docs/IMPLEMENTATION_PLAN.md`에 "Phase 4: 실사용 피드백 대응" 섹션을 미착수(`[ ]`) 상태로 먼저 추가하고, 구현 단위·검증 방법·빌드/커밋까지 9개 항목을 TaskCreate로 등록. 이 시점까진 아직 사용자의 기술적 내용 승인을 받지 않은 상태였음.
4. 사용자가 기술 내용에는 이견이 없다고 확인한 뒤, **같은 실수의 재발 방지를 위해 CLAUDE.md 자체를 먼저 업데이트하라고 요청**. AI가 "구현 계획과 커밋 단위" 절에 "계획은 채팅 응답 텍스트로 끝내지 않는다 — 제시 시점에 IMPLEMENTATION_PLAN.md 반영과 TaskCreate 등록을 함께 한다"는 문단과, 커밋 시 체크박스·TaskUpdate 상태를 함께 갱신한다는 문장을 추가.
5. TaskUpdate로 진행 상태를 실시간 갱신하며 Unit 1을 구현 — `responseJsonExtractor.ts`/`errorCaseParser.ts`(옵션 파라미터 `pageLabel` 추가) → `propertyExtractor.ts`(이미 계산돼 있던 `displayName`을 내부 헬퍼에 전달, 외부 시그니처 불변) → `pageParser.ts`(`pageLabel = \`${displayName} (${page.url})\`` 조합 후 하위 전달) 순으로 의존관계에 맞춰 진행.
6. `npm run build` + `npm test`(기존 55개, 시그니처 변경 없이 전원 통과 — 새 파라미터가 옵션이라 기존 호출부 영향 없음을 사전에 설계로 보장) 확인 후, 실 DB로 `npm run dev`를 두 차례 재실행해 서로 다른 경고 타입(응답코드 파싱 실패, 응답 JSON 복수 후보, 유효하지 않은 JSON)이 실제로 `[페이지 제목 (Notion URL)]` 접두사와 함께 출력되는 것을 육안으로 확인.
7. 커밋을 계획과 별개인 CLAUDE.md 규칙 변경(1커밋)과 Unit 1 소스 구현(1커밋)으로 분리 — 전자는 이번 세션에 사용자가 별도로 명시 요청한 작업이라 애초의 Unit 1 계획 범위 밖이었음을 스스로 인지하고 커밋을 나눔. 두 커밋 모두 완료 즉시 `IMPLEMENTATION_PLAN.md` 체크박스를 `[x]`로, TaskUpdate 상태를 `completed`로 갱신.

**포트폴리오 관점에서의 핵심 포인트**:
- 이 세션의 핵심은 기술적 버그 수정이 아니라 **AI가 자기 자신이 따라야 할 프로젝트 규칙을 어긴 것을, 사람이 규칙 문서(CLAUDE.md)를 근거로 직접 지적한 사례**다. AI는 실수를 인정하는 데 그치지 않고, 같은 실수가 재발하지 않도록 규칙 문서 자체를 사람의 지시로 보강했다 — 결과에 대한 수정이 아니라 프로세스에 대한 수정이라는 점에서 세션 3/5(리뷰 검증 습관을 코드에 적용)와는 다른 층위의 교정.
- "계획을 세웠다"는 것의 정의를 "채팅으로 설명함"에서 "문서(IMPLEMENTATION_PLAN.md)와 태스크 트래커에 반영함"으로 명확히 좁힌 것 — 모호했던 규칙이 실제 위반 사례를 계기로 더 구체적이고 검증 가능한 형태로 진화함.
- 계획 범위 밖의 작업(CLAUDE.md 수정)이 같은 세션에 끼어들었을 때, 이를 기존 계획(Unit 1)에 슬쩍 얹지 않고 별도 커밋으로 분리한 것 — CLAUDE.md 스스로가 명시한 "계획에 없던 범위로 커밋하지 않는다" 원칙을, 그 규칙을 막 새로 쓴 직후에 바로 스스로 지킨 사례.
